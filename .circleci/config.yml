version: 2.1
jobs:
  # go project build
  build:
    docker:
      - image: golang:1.12-stretch
    working_directory: /go/src/github.com/countableset/swimming-goggles
    steps:
      - checkout
      - run: make
      - store_artifacts:
          path: app
          destination: app

workflows:
  version: 2
  build-and-package:
    jobs:
      - build
